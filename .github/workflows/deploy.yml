name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: 22
          script: |
            mkdir -p ~/prod
            cd ~/prod
            if [ ! -d "hankyung-consensus-report" ]; then
              git clone git@github.com:${{ github.repository }}.git hankyung-consensus-report
            fi
            cd hankyung-consensus-report
            git fetch origin main
            git reset --hard origin/main
            docker-compose up -d --build
